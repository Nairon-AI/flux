---
name: flux:setup
description: Optional local install of fluxctl CLI and project docs
---

# IMPORTANT: This command MUST invoke the skill `flux-setup`

The ONLY purpose of this command is to call the `flux-setup` skill. You MUST use that skill now.

This is an **optional** setup for power users. Flow-next works without it.
