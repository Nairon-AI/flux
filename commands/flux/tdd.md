---
name: flux:tdd
description: Test-driven development with red-green-refactor vertical slices.
argument-hint: "<task-id|feature|bugfix>"
---

# IMPORTANT: This command MUST invoke the skill `flux-tdd`

The ONLY purpose of this command is to call the `flux-tdd` skill. You MUST use that skill now.

**User request:** $ARGUMENTS

Pass the user request to the skill. The skill handles the TDD loop and related planning.
