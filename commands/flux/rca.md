---
name: flux:rca
description: Root cause analysis for bugs, regressions, and broken behavior.
argument-hint: "<bug description|error|incident>"
---

# IMPORTANT: This command MUST invoke the skill `flux-rca`

The ONLY purpose of this command is to call the `flux-rca` skill. You MUST use that skill now.

**User request:** $ARGUMENTS

Pass the user request to the skill. The skill handles reproduce → investigate → root cause → fix → verify → learn.
