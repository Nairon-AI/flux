---
name: flux:grill
description: Behavioral stress test for completed epics and implemented workflows.
argument-hint: "[epic-id|feature description]"
---

# IMPORTANT: This command MUST invoke the skill `flux-grill`

The ONLY purpose of this command is to call the `flux-grill` skill. You MUST use that skill now.

**User request:** $ARGUMENTS

Pass the user request to the skill. The skill handles behavioral verification and decision-tree stress testing.
