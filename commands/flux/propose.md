---
name: flux:propose
description: Stakeholder-facing feature proposal flow with engineering pushback and handoff.
argument-hint: "<feature idea or problem statement>"
---

# IMPORTANT: This command MUST invoke the skill `flux-propose`

The ONLY purpose of this command is to call the `flux-propose` skill. You MUST use that skill now.

**User request:** $ARGUMENTS

Pass the user request to the skill. The skill handles stakeholder intake, pushback, estimation, and proposal handoff.
