---
name: flux:design-interface
description: Compare multiple interface designs for a module or API using parallel sub-agents.
argument-hint: "<module|API|boundary to design>"
---

# IMPORTANT: This command MUST invoke the skill `flux-design-interface`

The ONLY purpose of this command is to call the `flux-design-interface` skill. You MUST use that skill now.

**User request:** $ARGUMENTS

Pass the user request to the skill. The skill handles requirement gathering, variant generation, and synthesis.
