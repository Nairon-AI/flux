---
name: flux:remember
description: Explicit memory capture alias for remember/don't forget requests.
argument-hint: "<thing to remember>"
---

# IMPORTANT: This command MUST invoke the skill `flux-remember`

The ONLY purpose of this command is to call the `flux-remember` skill. You MUST use that skill now.

**User request:** $ARGUMENTS

Pass the user request to the skill. The skill handles classification and delegation to Flux memory storage.
