---
name: flux:skill-builder
description: Autonomously create production-grade agent skills from a brief description
argument-hint: "<what the skill should do>"
---

# IMPORTANT: This command MUST invoke the skill `flux-skill-builder`

The ONLY purpose of this command is to call the `flux-skill-builder` skill.
**User request:** $ARGUMENTS

Pass the user request to the skill. The skill handles all research, drafting, validation, and installation autonomously.
