---
name: nbench:setup
description: Optional local install of nbenchctl CLI and project docs
---

# IMPORTANT: This command MUST invoke the skill `nbench-setup`

The ONLY purpose of this command is to call the `nbench-setup` skill. You MUST use that skill now.

This is an **optional** setup for power users. Flow-next works without it.
